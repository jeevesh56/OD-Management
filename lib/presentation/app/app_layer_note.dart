/// Presentation layer entry point.
///
/// Existing screens currently live in legacy `lib/` paths.
/// New UI modules should be created under:
/// - `lib/presentation/screens`
/// - `lib/presentation/widgets`
///
/// Business rules and persistence are now separated into:
/// - `lib/domain`
/// - `lib/data`
class AppLayerNote {
  const AppLayerNote._();
}
